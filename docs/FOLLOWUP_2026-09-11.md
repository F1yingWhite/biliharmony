# 2026-09-11 待办收尾与虚拟机验证

本轮从动态加载与播放器验收开始，继续补齐 PGC 取流。基线为 `c4b1f6a`；下列改动在工作区，尚未提交或推送。证据保存在忽略目录 `.qa/followup-20260911/`。

## 已修复

- 网络层保留每次请求独立的 HarmonyOS 错误码，区分 DNS、连接、超时、证书与响应过大。日志只包含数值错误码，不记录请求地址、Cookie 或异常原文。动态首屏显示对应恢复提示，畸形响应不再当成成功空列表。
- 动态评论切换排序保留旧评论，收到当前请求的新结果后整体替换，避免清空长列表导致滚动位置与排序按钮移动。加载中显示指示，失败明确提示保留的是原评论，并可重试新排序首屏。
- 番剧选集保留 `epId/cid`；初次取流和换画质均使用官方 PGC 接口。以服务端集信息确定 CID，普通投稿详情仅补齐 UP 和统计。显示试看标记与权限错误；DRM 和多段旧格式明确提示当前不支持；番剧下载仍未开放。
- 构建脚本使用 DevEco 自带 Java；构建失败立即停止，不再因为输出目录残留旧 HAP 而继续安装。优先选择不早于 unsigned 包的 signed 包。
- GitHub Actions 改为运行 regression、lifecycle、danmaku、parity 四组测试。

## 当前验证

- 119 项源码回归通过，覆盖并发网络错误归属、动态分页/排序恢复、PGC 集身份、试看、权限拒绝及不支持的格式。
- API 26 Release 编译、打包与签名成功，构建产物 `DEBUG=false`。当前本机 `.p12/.cer/.p7b` 均存在；旧文档中“本机缺签名证书”的阻断已过时。
- 已覆盖安装到用户指定的虚拟机 `127.0.0.1:5555`，保留登录数据。
- 动态流已显示正常内容；视频、番剧、专栏、全部分类可切换。此前 `status=-1/code=-1` 的传输故障未复现，不能把此次恢复归因于新增诊断代码，也不能推断旧故障原因。
- 动态评论“最新→最热→最新”实屏验证：排序按钮位置保持 `[1099,327][1260,406]` / `[911,327][1072,406]`，首条评论随排序更新。对应 `sort-fixed-latest.jpeg` 与 `sort-after-*.json`。
- 普通 Bad Apple 视频观察到播放画面、密集弹幕与递增进度；Surface 与弹幕视口同为 `[0,132][1320,874]`。没有据此宣称音画同步或性能验收完成。
- 普通视频进度条拖动与画面横滑后，实际进度分别为约 85 秒、136 秒，松手后预览均消失。旧自动化脚本的全屏点击超时，随后按当前控件坐标验证 PGC 横竖屏正常；不把脚本超时归为播放故障。
- 从搜索的影视结果进入《让子弹飞》，选第二个“四川话”版本，显示正确标题、UP、统计、实际电影画面与弹幕。横屏 `2848×1320`，控制栏显示 `1080P`、总长 `2:12:10`。这是当前登录账号与该样本的验证，不能扩展为所有会员、付费或地区内容均可播放。
- 直播“流泪鸡蛋饼”房间显示实际画面、文字与图片表情；进入横屏 `2848×1320`，系统返回恢复竖屏 `1320×2848`。未在此次窗口内完成真实 SC 展开/倒计时验证。

PGC 接口依据：2026-09-11 读取 [官方播放页](https://www.bilibili.com/bangumi/play/ep199612)，并直接核验官方 `pgc/view/web/season` 与 `pgc/player/web/playurl` 响应；未登录请求返回 `is_preview=1`，当前虚拟机账号另行验证正常播放。没有绕过权限或使用第三方解析源。

## 尚未完成

- 旧动态传输故障的具体原因仍未知；再次发生时可用新增错误码继续定位。
- 试看标签、拒绝播放和排序失败状态已做源码测试，尚未对当前登录设备注入这些条件。
- 真实 SC 展开/倒计时、后台音频、跨视频手动画质记忆、章节跳转及尚未覆盖的设备/页面组合仍需进一步验收。
- 小窗、投屏、独立微信/QQ 分享与私信持续接收仍是后续功能。当前改动不代表全客户端对齐完成。
- 仓库尚无注册的 Actions runner，流水线无法开始运行；工作流修改不等于云端构建已通过。
