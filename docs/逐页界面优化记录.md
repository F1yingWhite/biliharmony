# 逐页界面优化记录

范围：23 个路由页面，以及首页、动态、我的三个主视图。包含本轮修改及此前已完成的首页、私信、通用组件优化。逐页代码检查完成不代表所有网络状态、账号状态、设备尺寸都已实屏验收。

## 剩余验收与阻断

- 专栏已实屏读到正文末尾并显示图片；深色正文与图片已实屏确认；其他文章格式和链接跳转仍需验收。
- 全站排行榜本次已恢复并完成正常内容验收；此前业务码 -352 为历史故障，不能保证远端始终可用。
- 登录各方式、收藏夹有内容/排序、图片原图/GIF/捏合，以及尚未覆盖的搜索分类与深色状态仍需实屏检查。
- 视频/直播故障重试入口已实现；故障注入与恢复后的实际播放尚未覆盖所有路径。
- 本机正式签名缺少 .p12；unsigned HAP 可打包并已安装到虚拟机。
- 当前实屏截图验证布局与具体操作，不证明真机帧率、全部尺寸或所有网络状态。

## 当前逐页状态

| 页面 | 已落实的优化 | 验证范围 |
| --- | --- | --- |
| Index / 主导航 | 保留页面与滚动位置，导航动效、点击反馈与悬浮入口检查 | 虚拟机主标签切换、详情返回 |
| HomeView / 首页 | 卡片间距与按压反馈、对齐卡片的网格骨架、固定高度分页尾部、空批次继续请求 | 此前虚拟机骨架对齐与连续下滑；本轮首页正常内容 |
| DynamicView / 动态 | 社交卡片骨架、空态/错误态 | 此前虚拟机加载与内容检查 |
| MineView / 我的与设置 | 独立设置页骨架、统计三列等宽、长昵称省略、显示模式与主题色触控区域、时间选择器自适应宽度 | 实屏统计、设置行与深色，恢复跟随系统；时间抽屉等宽双列/24 小时制已实屏确认，未改动原时间段 |
| Search / 搜索 | 各分类骨架、标签/实体清理、分页失败保留内容并重试、结束反馈、直播双列间距及跨列页尾、分类计数响应更新 | 实屏综合、用户、直播、专栏、番剧、影视列表；分类计数随请求更新，影视单条结果与结束提示正常；搜索竞态及分页行为测试通过；深色综合/影视及分类栏实屏确认，其他深色分类待核验 |
| Messages / 消息与私信 | 会话骨架、完整长文本、图片适配、气泡和时间、进入会话定位最新消息 | 此前私信显示检查、消息模型测试；未发送消息 |
| RankPage / 排行榜 | 列表骨架、入站必刷分页与同页重试、离页失效、底部安全区、三位排名单行 | 实屏每周必看、入站必刷至结束；全站榜恢复后检查首屏至第 100 名，详情返回保留榜尾位置 |
| ZoneChannelPage / 分区 | 单一返回栏、分类滚动、空态与失败分开、切换使旧请求失效 | 实屏动画/游戏分类及返回；请求交错测试通过 |
| LiveZonePage / 直播分区 | 紧凑直播行、匹配骨架、分类滚动、旧请求失效、分页失败重试 | 实屏网游/手游分类与返回；分页和旧响应测试通过 |
| VideoDetail / 视频详情 | 详情与评论骨架、播放地址失败重试、评论排序请求保护与分页错误反馈 | 虚拟机正常播放、简介与评论切换；失败重试未注入网络故障实测 |
| LiveRoom / 直播间 | 播放失败重试、画质失败保留播放、返回按钮层级、全屏重入取消旧回调 | 实屏两个直播间画面/实时弹幕、竖屏返回、全屏进入与退出；失败重试按钮尚未故障注入实测 |
| UserSpace / 用户空间 | 头图与资料骨架、投稿网格骨架、标签动效 | 虚拟机头图、资料和投稿内容；加载态编译检查 |
| DynamicDetail / 动态详情 | 评论骨架、评论入口定位到评论区开头、追加页失败保留内容并重试 | 实屏评论区定位与图片查看返回；分页失败/同页重试测试通过 |
| BangumiDetail / 番剧详情 | 详情与评论骨架、集标题省略、固定选集封面比例以修复整行异常增高、按类型区分追番/追剧及集数单位、评论切换防串页与分页失败重试 | 虚拟机确认修复前后选集布局，多行集数/时长/标题正常 |
| BangumiListPage / 我的追番 | 分类切换清掉旧列表、请求中防止分类错位、空态与错误重试分开、去除顶部多余空白 | 虚拟机列表与进入详情；错误状态代码检查 |
| BangumiIndexPage / 番剧索引 | 修正国创/电影分类编号及高亮、筛选立即切换、最新请求生效、三列页尾、失败重试 | 实屏国创＋已完结的高亮/海报/集数；竞态与同页重试测试通过 |
| BangumiTimelinePage / 新番时间表 | 日期时间戳排序、服务端今天标记、集数与分类修正、并行请求、失败反馈及离页失效 | 实屏日期分组/今天/时刻/集数；跨年合并与字段解析测试通过 |
| HistoryPage / 历史 | 列表骨架、账号准备阶段保持加载状态 | 虚拟机实际历史列表；已有分页错误与请求取消测试 |
| WatchLaterPage / 稍后再看 | 账号准备期间保持骨架、离页请求失效、底部安全区 | 实屏两条内容；相关生命周期测试通过，未执行删除或清空 |
| FavoritesPage / 收藏 | 按当前收藏夹/收藏集/视频判断加载与空态，独立错误提示、分页失败保留列表并手动重试；接口不再吞掉失败 | 虚拟机空态；新增接口失败与合法空列表区分测试 |
| FavoriteFolderSortPage / 收藏夹排序 | 上下移触控区域、保存期间禁止重排、保存异常提示、无项目空态 | 编译与代码检查；未修改账号排序 |
| RelationList / 关注与粉丝 | 头像骨架、接口失败与空态分开、分页同页重试、离页失效、长昵称省略 | 实屏关注列表；请求和错误行为测试通过 |
| BlackListPage / 黑名单 | 头像骨架、分页错误重试及离页失效、解除按钮尺寸与忙碌禁用 | 实屏黑名单列表；未执行解除拉黑 |
| ArticlePage / 专栏 | 直接获取正文、手机阅读排版、加载骨架与失败重试、原生图片下载、离页请求失效 | cv2015124 实屏标题/作者、正文首段至文末、首尾图片正常；深色标题/正文/图片与空段落收紧已实屏确认；其他格式及链接返回待验收 |
| ImageViewer / 图片查看 | 原图按钮安全区、无障碍标签、放大返回先恢复变换再缩回缩略图 | 实屏打开/放大/返回评论原位置；返回时序测试通过；GIF、捏合与原图操作仍待专项核验 |
| Login / 登录 | 二维码等待状态使用真实加载指示，登录请求期间避免切换登录方式 | 编译与代码检查；未退出当前账号或发起短信 |

验证：最新 ArkTS 编译与 HAP 打包通过；2026-09-10 整体回查运行 lifecycle/regression/parity/danmaku 四个行为测试文件，全部通过。虚拟机安装的是本次构建的 unsigned HAP。正式 SignHap 仍受本机缺失 .p12 证书影响，不能声称正式签名构建成功。自动测试用于数据和请求行为，不作为视觉流畅度或帧率的证明。

## 历次检查记录（当前结论以上表为准）

后续检查：直播分区已加入请求失效保护、分类列表滚动、单一返回栏、分页失败保留列表和同页重试；接口错误不再作为空直播列表。虚拟机验证网游分区、返回分类及切换手游分区；针对旧响应与分页失败重试的行为测试通过。直播分区随后改为紧凑横向直播行，固定 78vp 封面高度，并与列表骨架统一封面宽度；虚拟机首屏能展示约七个直播间。

骨架与布局复查：番剧索引新增三列、0.7 宽高比的海报骨架，去除额外 40vp 顶部空白；海报标题保留两行高度，列表增加行间距。新番时间表已实屏确认日期分组与节目列表。排行榜补充底部安全区，底部实屏检查仍待完成。

个人内容页复查：关注/黑名单接口失败不再返回成功空列表；分页失败显示重试并保留原页码、内容，页面销毁后旧请求失效。稍后再看增加账号准备与内容请求的离页失效处理及底部安全区。相关生命周期测试通过。虚拟机确认关注列表、稍后再看两条内容和黑名单状态，未执行清空、删除或解除拉黑。

排行榜复查：三个标签已实屏进入；每周必看正常展示期次，入站必刷已滑到第 98 条与结束提示，底部安全区正常。入站必刷新增 page/100 条分页、同页失败重试和结束判定；当前远端只有不足一页的结果，跨页续载由生产方法行为测试验证。三个榜单的请求均加入离页保护。全站排行显示错误重试页，独立公开接口请求也返回业务码 -352，正常榜单内容未验收，不能标为完成。

分区频道复查：分类列表可滚动，合并重复返回栏；返回或切换分区会使旧请求失效，针对新旧响应交错的测试通过。虚拟机确认动画、游戏分区正常加载及分类返回。专栏页新增主文档 HTTP 4xx/5xx 错误反馈，本地 SDK 类型和 ArkTS 编译通过；网页错误注入尚未实屏验证。

搜索复查：追加页错误与首屏错误分开，分页失败保留内容、同页重试，结果总数或空页决定结束；通用分类列表与综合页均接入结束反馈。虚拟机验证综合结果及用户搜索五条结果后的“没有更多结果了”。搜索结果的标题、名称、简介统一移除高亮标签并单次解码 HTML 实体，覆盖命名实体、数字实体、非法码点与避免双重解码的测试。编译打包、行为测试通过并安装到虚拟机；全部分类与深色实屏仍待核验。

搜索布局复查：直播双列补齐横向与组内纵向 10vp 间距，分页 footer 改为跨列分组尾部；用户结果采用头像列表骨架。直播分类与主播名称也清理高亮标签，长分类省略。最新构建已安装，实屏确认双列间距及分类纯文本；跨列 footer 结构编译通过，直播结束态尚未实屏验证。完整行为测试通过。

专栏实屏复查：从搜索结果进入文章，实际显示加载失败说明与重新加载按钮；点击重试后仍加载失败，正文正常状态仍未验收。搜索专栏列表的标题、摘要、缩略图与长标题省略显示正常。

专栏故障定位：设备日志确认 Web 主文档错误 -302（未知 URL 协议），来自网页唤起客户端。新增主框架非 HTTP(S) 导航拦截，忽略未知协议/取消导航导致的错误，避免正常网页被错误态遮挡；重试明确重新加载原文章地址。Web 使用移动浏览器标识，HTTP 404 与暂时不可访问分开提示。ArkTS 编译、打包及虚拟机安装通过，实屏已显示移动版标题、作者和正文预览。点击展开全文时，站点弹出“打开 APP 即可浏览完整内容”，全文与正文图片尚未完成应用内阅读验收，需要后续处理。

动态详情复查：修复点击动态评论入口后跳到列表最底部，改用评论区标题实际布局位置定位，虚拟机已确认首条评论与排序栏从顶部显示。追加评论失败显示重试，保留内容与游标，新增同页重试及结束停止请求的行为测试通过；分页页尾统一加载指示与结束提示。ArkTS 编译、打包及安装通过。

图片与深色复查：评论图片已实屏打开并返回原评论位置；放大状态返回过渡截图出现双层图像，转场仍需复查。首页与“我的”设置深色模式已实屏检查，文本、卡片、主题控件可辨认，检查后恢复原来的跟随系统模式；其他页面深色状态未据此推定验收通过。

图片缩放返回修复：ZoomableImage 在退出请求时先用 180ms 恢复缩放与偏移，完成后才启动缩回缩略图的动画，避免原尺寸 hero 叠在放大图片上。父页面防止重复触发返回。新增生产方法行为测试验证恢复完成前不开始返回；ArkTS 编译、打包通过，最新包已安装。虚拟机实际双击放大后返回，采样过渡帧未出现此前的双尺寸叠图，最终回到同一评论位置；该检查不代表所有 GIF、捏合手势或设备帧率已验收。

直播间复查：画质切换无地址或请求失败时提示并保留原播放源；离页后的旧失败不弹提示。播放器所有线路失败时新增重新加载按钮，调用房间同步流程并显示忙碌禁用。修复竖屏返回按钮被播放器 zIndex 盖住的问题。ArkTS 编译、打包、相关行为测试通过并安装到虚拟机；已实际进入两个直播间，确认画面与实时弹幕正常，返回按钮可见并点击返回。播放失败按钮的故障注入实屏与全屏仍待验收。

直播全屏复查：虚拟机确认横屏画面、显示控件、系统返回恢复竖屏与实时弹幕。修复退出全屏 220ms 延迟回调覆盖快速重新进入状态的问题，重新进入时取消旧定时器，回调也检查当前全屏状态。针对真实方法的快速退出/重入测试通过；编译、打包通过并安装最新版本。离页清理代码已检查播放器 release、弹幕 renderer、定时器和音量监听，设备资源占用未单独量测。

专栏全文再次核验：尝试桌面浏览器标识，主文档请求在设备 Chromium 日志返回 ERR_NAME_NOT_RESOLVED（-105），重试仍未恢复，无法用此次结果判断桌面版全文是否可用。已撤回桌面 UA 实验，恢复此前实屏验证过的移动版；全文阅读继续待办。

番剧索引复查：修复国创与电影编号颠倒（国创 4、电影 2），以及 Builder 值参数导致筛选高亮不随状态更新的问题；虚拟机确认“国创 + 已完结”高亮与国产动画列表一致。列表改用三列分组尾部，分页提示跨列；首屏接口错误不再伪装为空列表，分页错误保留原内容/页码并提供手动重试。实例级 RequestEpoch 替换静态请求序号，筛选可立即切换且仅最新响应提交，离页使旧响应失效。相关并发与同页重试测试、ArkTS 编译、打包通过，最新包已安装。

新番时间表复查：使用服务端 date_ts 排序与 is_today 标记，避免月份/跨年字符串排序问题；合并后按播出时刻排列当天节目。解析 pub_index 显示集数，并按请求 types 设置分类，修复国创被默认标为番剧。接口业务失败不再返回空排班；番剧/国创请求并行，页面离开或新请求启动后旧结果失效。跨年合并与集数字段测试、ArkTS 编译、打包通过，安装后实屏确认今天标记、日期分组、播出时间与集数。

设置时间抽屉复查：实屏发现原布局标签越界、两个选择器宽度不一致。已改为等宽双列、标签上置、固定 160vp 选择区、24 小时制和主题色选中数字。编译打包与安装通过，实屏显示完整 19:00/07:00、取消/确定按钮和安全底部间距；取消后关闭定时深色，恢复测试前开关状态。

搜索分类实屏复查：明日方舟番剧结果的封面、标题、评分与简介正常。修复标签 builder 接收计数字符串后未更新的问题，改为直接读取状态；安装后搜索“流浪地球”，确认视频/影视等分类数量更新，影视分类单条结果与结束提示正常。ArkTS 编译与 HAP 打包通过，正式签名仍缺本机证书。

影视详情文案复查：模型读取内容类型，番剧/国创保留追番与“话”，影视使用追剧与“集”；总数改为“共”避免暗示已经完结，加载失败与分享提示改为通用详情文案。选集更新状态可伸缩并省略。虚拟机“流浪地球2”确认追剧按钮、统计和“共 1 集”显示正常，未执行追剧操作。ArkTS 编译/打包及安装完成，正式签名仍缺证书。

番剧/影视评论复查：修复旧评论请求进行中切换选集或排序时新请求被拦截、旧响应写入当前列表的问题。请求代次保护覆盖成功、失败和 loading 收尾，离页使旧请求失效；分页错误保留内容及游标，统一页尾显示重试或结束状态。实际生产方法的请求交错、失败后同游标重试验证通过；虚拟机“流浪地球2”切换最新排序后高亮和新评论正常。ArkTS 编译与打包通过并已安装；网络失败页尾尚未故障注入实屏验收。

视频评论复查：加载中切换排序立即发起新请求，旧成功/失败/收尾均不能覆盖新状态，离页使请求失效。追加页失败保留列表和游标，页尾显示点击重试并暂停自动触底续载。实际生产方法的旧请求失败与排序交错、分页同游标重试测试通过；虚拟机连续切换最新/最热/最新，最终高亮与按时间排列的评论一致，视频播放正常。ArkTS 编译/打包通过并已安装，分页错误状态未故障注入实屏验证。

深色显示复查：搜索综合卡片、影视列表、影视详情的简介/评论/输入栏实屏可读。修复 Tabs 未明确背景色导致分类栏两端出现纯黑渐变的视觉断层，显式使用页面同色背景；安装后影视分类实屏确认黑色边缘消失。ArkTS 编译和打包通过。此次未覆盖全部深色分类与所有评论状态。

专栏正文阅读实现：从官方文章页所用脚本核实 `/x/article/view?id=...`，接口返回完整 HTML。新增 ArticleApi 与 ArticleHtml，应用内直接排版正文，转义标题/作者，禁用脚本和表单，正文 HTTPS 图片经现有原生网络通道加载；图片请求失败也会释放 Web 响应。处理初始空白页与 data HTML 导航，移除正文的空段落。虚拟机 cv2015124 已确认标题、作者、开头 GIF 图片、正文末段及结尾图片，未再出现打开 App 才能继续阅读的预览限制。四个行为测试文件通过，ArkTS 编译/打包和安装完成；正式签名仍缺证书。其他文章格式、深色与链接导航后续继续验收。接口依据为 https://www.bilibili.com/read/cv2015124/ 引用的 article-web 脚本，读取日期 2026-09-10。

专栏链接行为修复：用户点击正文 HTTP(S) 链接时交给系统打开，避免网页替换阅读正文；同篇文内锚点保留当前 Web 跳转，本地 HTML 装载照常通过，应用唤起协议仍被拦截。生产方法行为测试验证链接分流、锚点、初始化和协议拦截；ArkTS 编译/打包通过并安装。系统打开网页后返回的阅读位置尚需实屏验证，不能据单元测试宣称已验收。

专栏深色与段间距：实屏确认深色标题、作者、正文与图片可读，图片保持原始颜色。发现正文存在带 style/strong 的空段落，原先仅清理简单空段落，造成大片空白；扩展清理仅移除空白和排版标签组成的段落，正文与段内图片仍保留。修复后同篇深色实屏确认间隔正常，生产渲染测试和编译打包通过并安装。当前样本没有链接，不能作为链接打开/返回的实屏证据。

全站榜恢复验收：核对官方热门排行网页脚本后，确认仍使用 ranking/v2 的 rid=0/type=all；本次接口返回 code=0 和 100 条。虚拟机实屏确认全站首屏至第 100 名，并发现“100”在 26vp 序号列换行；改为 34vp 且限制单行，安装后确认完整显示。进入第 100 项详情再返回，榜尾位置保留。失败提示改为暂时无法加载，避免将服务端失败一律描述为用户网络问题。编译/打包及安装通过，正式签名仍缺本机证书；此次是恢复后正常路径验证，不代表远端故障不会复发。

动态详情评论并发修复：允许加载中切换排序，以请求代次屏蔽旧响应和旧 loading 收尾；离页使旧请求失效。动态资料补全不再重复发起正在进行的评论首屏请求。失败重试区分首屏刷新和追加页，已到末页后的刷新失败仍保留重试入口。生产方法请求交错与原分页重试测试通过，编译打包与虚拟机安装完成；本次快速切换的实屏验证尚待补齐。

动态流失败状态修复：本轮实屏进入动态流出现空列表，检查确认 parseDynamicPage 会把失败响应变为成功空列表。改为抛出加载错误；空的成功响应仍正常显示空态。追加页失败保留卡片、offset 和 hasMore，页尾显示可点击重试并暂停自动触底加载，刷新失败也保留正确的重试方式。失败/空结果区分及同 offset 重试生产方法测试通过，四个行为测试文件通过，编译打包和安装完成。当前实屏空列表不足以证明该次请求具体失败原因；动态详情连续排序实屏验收仍未补齐。

动态失败实屏与原因核验：新版实屏显示加载失败和重新加载入口，重试仍未恢复。增加仅含状态码/业务码的诊断日志，设备记录 status=-1/code=-1，证明本次未收到正常 HTTP 响应，不应当作空列表或推断账号已失效。另区分服务端 -101 的登录失效文案，测试覆盖普通失败、登录失效和成功空结果；编译打包及安装通过。动态正常内容与连续评论排序的本轮实屏验收仍未完成。

动态登录失效恢复操作：LoadingView 支持自定义重试文案；动态首屏和分页在明确收到登录失效后显示“去登录”，点击进入已有登录页，普通失败继续重试。已有卡片不清空。恢复动作测试覆盖登录导航与分页重试方式，编译打包及虚拟机安装完成。当前设备观察到的是传输失败而非 -101，登录失效按钮的实屏状态尚未注入验证；未退出账号或发起登录请求。

搜索海报骨架匹配：番剧/影视不再复用横版视频列表骨架，新增 mediaList，海报 84×112、左右 14vp、行内间隔 10vp、上下各 8vp，与 PgcRow 一致；文字占位对应标题、评分、分类和简介。编译/打包通过并安装，虚拟机番剧结果正常。此次截图捕获到切换过渡与最终内容，未捕获稳定加载骨架帧，骨架视觉对齐仍需补做实屏验证。
